apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  # In production, this would be injected by a secure vault
  # Never commit real secrets to git - this is just for learning
  DB_PASSWORD: "apppassword"
